CREATE TABLE wines(
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    wineCat TEXT NOT NULL,
    date INTEGER NOT NULL,
    company_name TEXT NOT NULL,
    name TEXT NOT NULL,
    content TEXT,
    rating INTEGER
)